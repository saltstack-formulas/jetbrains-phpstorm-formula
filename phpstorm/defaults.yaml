#Default Look Dictionary

phpstorm:
  prefix: /usr/lib/jetbrains
  tmpdir: /tmp/jetbrainstmp
  command: /bin/phpstorm.sh
  homes: /home
  osflavour: linux

  jetbrains:
    home: /opt/jetbrains
    uri: https://data.services.jetbrains.com/products/releases?latest=true&code=
    #See https://www.jetbrains.com/help/phpstorm/Introduction.html
    #Default is phpstorm
    product: PS 
    edition:

  dl:
    archive_type: tar
    opts: -s -L
    interval: 60
    retries: 1
    source_url:
    src_hashfile:
    src_hashsum:

  linux:
    altpriority: 170
    symlink: /usr/bin/phpstorm

  prefs:
    user: undefined_user
    jarurl:
    jardir:
    jarfile: phpstorm-settings.jar

